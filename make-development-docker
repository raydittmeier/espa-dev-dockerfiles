# Setup
tag=:201608
source=development.docker
name=$USER.centos.development.docker${tag}
flat_name=$USER.centos.development.docker.flat

# Build the image
docker build -t ${name} ${source}

# Now Flatten the image
#ID=$(docker run -d $NAME /bin/bash)
#echo "ID = "$ID
#docker export $ID | docker import - ${flat_name}

# Remove the container we used
#docker rm $ID
